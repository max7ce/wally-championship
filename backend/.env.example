# Database Configuration (Railway PostgreSQL)
DATABASE_URL=postgresql://user:password@host:port/database

# JWT Secret (generar con: openssl rand -hex 32)
SECRET_KEY=tu_secret_key_super_seguro_aqui_cambiar_en_produccion

# CORS Origins (separar con comas)
CORS_ORIGINS=http://localhost:5173,https://tu-frontend.railway.app
